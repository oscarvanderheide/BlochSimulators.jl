# Documentation build is now handled by CI.yml
# This file is kept for backwards compatibility but does nothing
# You can delete this file if desired

name: Documentation (Deprecated)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Documentation moved to CI.yml
        run: echo "Documentation building has been moved to CI.yml workflow"